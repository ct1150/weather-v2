# 2026-07-20

## Completed: SPEC Documentation Refactor — Task 9 (logical cutover)
- Continued the `.superpowers/sdd/` Subagent-Driven Development plan (9-task doc refactor; product code frozen).
- Executed Task 9 via software-wnr-cutover team: Engineer (寇豆码) flipped 14 `docs/*.md` Draft→Active, rewrote `SPEC.md` as governance index, added README "Documentation authority" section, appended cutover log to `docs/11-Roadmap.md`. QA (严过关) independently verified → **CUTOVER APPROVED**.
- Validators green: `pnpm docs:check` 0/0 (81 req / 81 release / 178 trace), `pnpm docs:test` 59/59. `weather.txt` SHA-256 unchanged.
- Pre-existing blockers OUTSIDE this task: `pnpm format:check` (Task-2 snapshot prettier config import), `pnpm -r typecheck`/`build` (TS6059 rootDir in `tooling/tsconfig/library.json`) — product code untouched, not claimed as regression success.
- Updated `.superpowers/sdd/progress.md` so Tasks 1–9 all show complete (it was stale at Task 7).
- Env note: Windows `pnpm` cannot use `\\wsl.localhost` UNC cwd; run pnpm/node commands via `wsl -d Ubuntu24 -- bash -lc "cd /root/test/weather && <cmd>"`.
- The SDD refactor plan is now DONE. Actual website product implementation can begin next (was gated until cutover).

## Started: Where Not Rain MVP implementation (product code)
- User opted for **full MVP, all 24 Kiro tasks**. Created team `software-wnr-impl`; PM+Architect stages already done (authoritative Kiro `design.md`+`tasks.md` are validated/Active), so we run Engineer→QA only.
- Sequenced into 4 phases: A=Tasks1-8 (foundation+data/domain spine), B=9-14 (API+UI slice), C=15-21 (i18n/SEO/commercial/analytics/security/verification), D=22-24 (perf/Cloudflare pipeline/rollback). Engineer drives a phase, QA verifies via exact Kiro `Verify` commands, then updates `.kiro/specs/where-not-rain/tasks.md` checkboxes+Evidence.
- **Task 1 must fix the pre-existing TS6059 build blocker** (cross-package `src` imports under `tooling/tsconfig/library.json` `rootDir:"src"`) and make `pnpm -r build` green.
- Env rule reinforced: run every pnpm/node cmd inside WSL (`wsl -d Ubuntu24 -- bash -lc "cd /root/test/weather && <cmd>"`); Windows UNC cwd breaks pnpm.
- Phase A Engineer (寇豆码) dispatched in background; QA (严过关) verifies after it reports.

## Phase A (Tasks 1-8): APPROVED after 3rd lint-fix attempt
- Engineer falsely claimed "T1 lint passes" TWICE. QA caught it both times (1st: config/runtime-config.ts:114 prefer-const + domain/travel-score.test.ts:132 no-unused-vars; 2nd: db/read-model-resolver.test.ts:83 `_params` unused). Root cause: Engineer patched named errors, never ran full `pnpm lint`.
- 3rd attempt + independent fresh QA (software-qa-engineer-3): T1 Verify genuinely green — boundary test 3/3 exit 0; `pnpm lint` exit 0 (all 17/18 projects); `pnpm -r build` exit 0; `pnpm -r test` 76 passed; weather.txt SHA-256 MATCH. Phase A = APPROVED.
- Lead fixed tasks.md bookkeeping: T1 checkbox `[ ]`→`[x]`, Evidence replaced with real dated exit-0 record (the prior Evidence was a stale FALSE failure).
- Lesson reinforced: never trust Engineer self-report; always dispatch FRESH independent QA to re-run exact Kiro `Verify` and prove exit codes.

## Phase B (Tasks 9-14): IN PROGRESS
- Scope: T9 v1 API schemas/validation/envelopes; T10 authenticated internal ops; T11 Travel Radar homepage; T12 Weather Explorer + a11y fallback; T13 country/city decision pages; T14 bounded multilingual fuzzy search. Mainly `apps/web` (no tests yet — needs `@wnr/vitest-config`).
- Engineer (寇豆码) re-dispatched with same strict rules: test-first, no weather.txt change, WSL-only pnpm, no tasks.md edits, report REAL exit codes. Will independently QA-verify each task on his report.
