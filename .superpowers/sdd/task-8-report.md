# Task 8 Implementation Report

- Date: 2026-07-17
- Scope: synchronize the three Kiro-derived MVP specifications
- Repository mode: non-Git; no commit created

## Authority and Roadmap inventory

The Task 1 parsers read 81 Active Hard authority requirements and 81 unique Roadmap release records. Roadmap resolution selected exactly 64 MVP requirements and 17 future requirements (Beta, V1, or V2). All three Kiro manifests contain the same 64 MVP IDs grouped across 11 authority source files. Every digest was generated by `digestRequirementSelection` from `tooling/docs/requirement-format.mjs`; no digest was hand-calculated.

## Changes

- `.kiro/specs/where-not-rain/requirements.md`
  - Added the canonical derived JSON as the first line.
  - Replaced the legacy SPEC-numbered scope with 64 ubiquitous EARS statements, one occurrence per MVP authority ID.
  - Removed future requirements from normative scope and retained future IDs only as inline authority links under `## Out of current scope`.
- `.kiro/specs/where-not-rain/design.md`
  - Added the canonical MVP-only derived manifest.
  - Reconciled retained architecture, data-flow, provider-port, read-model, UI, SEO, security, performance, and deployment design into 22 implementation units.
  - Added valid MVP `_Requirements:` metadata to every level-three design unit and a complete 64-ID coverage appendix.
  - Removed legacy `SPEC §N` ownership and delayed-scope design obligations.
- `.kiro/specs/where-not-rain/tasks.md`
  - Added the canonical MVP-only derived manifest.
  - Replaced the mixed-release plan with 24 unchecked MVP task blocks.
  - Added real MVP IDs, executable `Verify:`, observable `Expected:`, and pending `Evidence:` fields to every checkbox block.
  - Added a complete 64-ID task coverage appendix and removed detailed Beta/V1/V2 tasks.

## Validation evidence

- Pre-edit staging baseline: `node tooling/docs/validate-docs.mjs --mode staging` exited 1 with 946 errors, 0 warnings; failures were concentrated in missing/stale Kiro manifests, bidirectional coverage, design metadata, and task fields.
- Red/green validator cycle: the first generated form exposed duplicate link-label/fragment ID counting and list-prefixed task fields; after correcting those source syntax causes, staging exited 0.
- Formatting: `pnpm exec prettier --write .kiro/specs/where-not-rain/requirements.md .kiro/specs/where-not-rain/design.md .kiro/specs/where-not-rain/tasks.md` exited 0. Prettier emitted the repository's existing module-type performance warning for `prettier.config.js`; formatting still completed successfully.
- Docs tests: `pnpm docs:test` exited 0 with 59 tests passed, 0 failed.
- Staging validator: `node tooling/docs/validate-docs.mjs --mode staging` exited 0 with 0 errors and 0 warnings; stats were 81 requirements, 81 releases, and 178 traces.

## Decision log

- Decision summary: derive the active MVP set exclusively by joining authority metadata to Roadmap release records, preserve implementation value in grouped design units, and keep future scope non-normative and link-only.
- Known limitations: authority documents remain Draft until the separate cutover task; no product implementation or external integration was performed.
- ADR: none — no new architectural decision
- Next step: Task 9 controlled cutover and active-mode validation.

## Review remediation — task8_fixer

- Date: 2026-07-17
- Scope remained limited to `.kiro/specs/where-not-rain/design.md`, `.kiro/specs/where-not-rain/tasks.md`, the three derived manifest headers, and this report. The requirements body and all product code remained unchanged.
- `.kiro/specs/where-not-rain/design.md` now restores implementation-oriented application use-case, ViewModel, repository-port, D1-active-first `ReadModelResolver`, typed key-codec, immutable CoreData, public API adapter, and `AsyncState<T>` shapes. The shapes delegate exact endpoint, key serialization, TTL, status, envelope, and authority contracts to their linked owner documents rather than copying those contracts.
- The resolver design reads authoritative D1 publication identity before deriving one immutable KV key, verifies hint/core identity and checksum, falls back only to active D1 rows, and exposes no user-path write, repair, provider, queue, or Cache API port. Obsolete KV-first and stable caller-selectable key behavior was not restored.
- Every one of the 24 tasks remains unchecked with pending Evidence. Each `Verify:` now starts with `test -f` guards for concrete production and test artifacts, then runs a targeted test and build (with task 1 additionally running repository boundary lint/build). This prevents an empty test suite from satisfying a task.
- Task 22 now requires the production web build, performance-gate tests, Lighthouse CI, and the 28-day route-class RUM evaluator. Task 23 now requires the production web build, preview contract tests, Pages preview compatibility verification, and representative preview smoke execution. Both tasks remain pending; this documentation-only remediation performed no performance or deployment integration.
- The manifest generator was rerun for `requirements.md`, `design.md`, and `tasks.md` using `parseRequirementBlocks` plus `digestRequirementSelection`. Each manifest retained the same 64-ID source selection and therefore the same canonical digest values; the generator reported all three manifest lines unchanged.

### Review-remediation validation evidence

- Formatting write: `pnpm exec prettier --write .kiro/specs/where-not-rain/design.md .kiro/specs/where-not-rain/tasks.md` exited 0. The existing module-type warning for `prettier.config.js` remained non-fatal.
- Task/manifest audit exited 0: 24/24 tasks were unchecked, pending, artifact-guarded, and targeted-test/build gated; task 22 contained both Lighthouse CI and 28-day RUM commands; task 23 contained preview contract and preview smoke commands; each Kiro manifest contained the same 64 IDs and every source digest recomputed fresh.
- Docs tests: `pnpm docs:test` exited 0 with 59 tests passed, 0 failed, 0 skipped.
- Staging validator: `node tooling/docs/validate-docs.mjs --mode staging` exited 0 with 0 errors and 0 warnings; stats remained 81 requirements, 81 releases, and 178 traces.
- Repository mode remained non-Git; no commit was created or claimed.

## Re-review remediation — task8_fixer_round2

- Date: 2026-07-17
- Scope remained limited to `.kiro/specs/where-not-rain/design.md`, `.kiro/specs/where-not-rain/tasks.md`, and this report. The derived authority selection did not change, so no manifest header update was required.
- Every Vitest artifact/argument now ends in `.test.ts`, and every targeted Vitest invocation passes `--passWithNoTests=false`.
- Every `node --test` gate now runs through Bash with `pipefail`, captures and re-emits Node TAP, restores the Node test process exit status on failure, and requires the emitted `# tests` count to be greater than zero on success.
- The D1 publication interface now returns `{ active: WeatherPublicationIdentity, publicationTokenHighWater: number }`. Resolver validation fails closed unless the active fencing token equals the high-water token, requires every active identity field to match exactly, and carries the same authority into D1 fallback.
- Task 23 remains unchecked with pending Evidence. Its exact Verify command now artifact-guards and runs a nonzero pipeline contract suite, builds an immutable artifact/ID, deploys that artifact to preview with a recorded bound URL/environment identity, applies preview migrations and repository checks, smokes the bound URL against the artifact ID, and performs a production promotion dry-run requiring same-artifact reuse and fail-closed mismatch handling.

### Re-review validation evidence

- Formatting: `pnpm exec prettier --write .kiro/specs/where-not-rain/design.md .kiro/specs/where-not-rain/tasks.md .superpowers/sdd/task-8-report.md` exited 0; the subsequent `--check` reported all matched files use Prettier style. The repository's existing module-type warning for `prettier.config.js` remained non-fatal.
- Docs tests: `pnpm docs:test` exited 0 with 59 tests passed, 0 failed, and 0 skipped.
- Staging validator: `node tooling/docs/validate-docs.mjs --mode staging` exited 0 with 0 errors and 0 warnings; stats remained 81 requirements, 81 releases, and 178 traces.
- Node TAP wrapper smoke: the exact Bash/awk wrapper ran `tooling/docs/requirement-format.test.mjs`, preserved exit 0, and observed `# tests 17` with 17 passes.
- Re-review static gate exited 0: 24/24 tasks remained unchecked and pending; all 21 Vitest gates used `.test.ts` plus `--passWithNoTests=false`; all three Node test gates used the TAP/count/exit-status wrapper; the D1 active/high-water invariants and every Task 23 artifact/deploy/migration/repository/smoke/promotion token were present; all three manifests retained one shared 64-ID source selection.
- Repository mode remained non-Git; no commit was created.
