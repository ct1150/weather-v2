"use client";

import type { ReactElement } from "react";
import { LocalizedTripWorkspace } from "./LocalizedTripWorkspace";

export function InternationalTripWorkspace(): ReactElement {
  return <LocalizedTripWorkspace locale="en" />;
}
