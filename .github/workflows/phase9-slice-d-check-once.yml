name: Phase 9 notification readiness check once

on:
  pull_request:

permissions:
  contents: write

concurrency:
  group: phase9-notification-readiness-check
  cancel-in-progress: true

jobs:
  verify:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.head.ref == 'feature/conversion-retention-phase9'
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/conversion-retention-phase9
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          version: 10.11.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Format Slice D
        run: |
          pnpm exec prettier --write \
            packages/domain/src/notification-readiness.ts \
            packages/domain/src/notification-readiness.test.ts \
            packages/domain/src/index.ts
      - name: Verify notification readiness
        run: |
          pnpm --filter @wnr/domain test
          pnpm --filter @wnr/domain build
      - name: Commit formatting and remove helper
        run: |
          rm .github/workflows/phase9-slice-d-check-once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat(retention): verify notification readiness model"
          git push origin HEAD:feature/conversion-retention-phase9
